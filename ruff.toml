# Le module app/tmcert/ est un kit EXTERNE, gardé tel quel : il sert de
# référence visuelle pour le certificat (voir app/tmcert/SPEC.md). On ne le
# reformate pas, sinon les mises à jour du kit deviendraient impossibles à
# rejouer. Le reste du projet garde les règles ruff par défaut.
exclude = ["app/tmcert"]
